//A pointer is a variable that holds memory address of another variable. 

//A reference variable is an alias or another name for an already existing variable. 

int i = 3; 

// A pointer to variable i or stores address of i
int *ptr = &i; 

// A reference ia an alias for i.
int &ref = i; 
